import "./App.css";

function App() {
  return (
    <main className="app-shell">
      <section className="app-intro" aria-labelledby="app-title">
        <p className="app-kicker">Local IDE shell</p>
        <h1 id="app-title">Agent Dock</h1>
        <p className="app-summary">Agent-aware workspace for local coding sessions.</p>
      </section>
    </main>
  );
}

export default App;
